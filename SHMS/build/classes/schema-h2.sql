
drop table USER;
CREATE TABLE USER (
	ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	ROC_ID VARCHAR NOT NULL,
	NAME VARCHAR NOT NULL,
	UNIT_ID VARCHAR NOT NULL,
	JOB_NAME VARCHAR NOT NULL,
	JOB_LEVEL BIGINT NOT NULL,
	PHONE VARCHAR,
	EMAIL VARCHAR,
	IS_LEAVE BIGINT NOT NULL
);

